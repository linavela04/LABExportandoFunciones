const sumar = (a, b) => a + b;
const restar = (a, b) => a - b;
const multiplicar = (a, b) => a * b;

const { PI } = require('./constantes');
const areaCirculo = (radio) => PI * radio ** 2;
module.exports = {sumar, restar, multiplicar, areaCirculo};

//Ejercicio 3

c
